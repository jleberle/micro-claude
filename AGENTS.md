# Agent session notes

See [`CLAUDE.md`](CLAUDE.md) — this repo keeps a single canonical set of
session notes there (theme history, platform quirks, Hugo version-compatibility
landmines). Read and update that file, not this one.

This file used to be a full independent copy of the same notes, edited under a
different project identity ("micro-Codex theme," repo "micro-Codex"). It
drifted from `CLAUDE.md` and that stale identity leaked into shipped code
(`footer.html`'s theme link, a `main.css` header comment) — fixed 2026-07-29.
Two hand-maintained copies of the same operational knowledge will always
re-drift, so don't recreate a second copy here.
